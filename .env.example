# supplyify has no runtime environment variables.
# This placeholder exists to satisfy the Army open-source readiness check.
#
# The tool reads:
#   - Command-line flags (see: supplyify --help)
#   - Indicator database bundled with the binary
#
# No API keys, no secrets, no runtime configuration outside the CLI.
